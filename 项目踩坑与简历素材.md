# 项目踩坑与解决记录（简历素材）

> 项目：AI 智能求职与面试平台（`ai-job-server`）  
> 用途：记录真实问题、原因、处理方式，方便写「项目难点 / 个人贡献」  
> 持续更新中

---

## 怎么用这份文档写简历

不要写「遇到了很多 bug」。优先写成：

- **问题现象** → **根因** → **方案** → **结果**
- 突出：环境排查、依赖选型、配置落地、分层架构理解

示例句式：

> 项目启动阶段排查 JDK 多版本冲突，统一切换至 Java 21，并完成 Spring Boot 3 + MyBatis-Plus 工程骨架搭建。

---

## 1. JDK 多版本并存，默认仍是 17

### 现象

- 方案要求 **Java 21**
- 执行 `java -version` 却是 **17**
- Maven 也绑定在 JDK 17 上

### 根因

机器上同时存在 JDK 8 / 17 / 21，但环境变量不一致：

| 配置项 | 曾出现的问题 |
|--------|----------------|
| 系统 `JAVA_HOME` | 已改成 `jdk-21` |
| 用户 `JAVA_HOME` | 仍指向 `jdk-17`（优先级更高） |
| 系统 `Path` | 仍优先命中 `jdk-17\bin` |

Windows 下：**用户级环境变量会覆盖系统级**；`java` 命令看 PATH，Maven 主要看 `JAVA_HOME`，两边不一致时就会「一半对、一半错」。

### 解决

1. 用户 / 系统 `JAVA_HOME` 都指向 `C:\Program Files\Java\jdk-21`
2. `Path` 中把 `jdk-21\bin` 放到 `jdk-17` 前面（或替换旧路径）
3. **新开终端** 再验证（旧窗口不会自动刷新环境变量）

### 验证

```text
java --version   → 21.0.8
mvn -version     → Java version: 21.0.8
```

### 简历可写

> 本地存在多 JDK 版本时，定位到用户级 `JAVA_HOME` 与 PATH 优先级问题，完成 Java 21 统一切换，保障 Spring Boot 3 工程可编译运行。

---

## 2. Spring Initializr 默认只提供 Boot 4.x

### 现象

- 项目明确要走 **Spring Boot 3.x + Java 21**
- 在线 Initializr 元数据默认已是 **4.x**，3.x 选项不可用

### 根因

官方脚手架版本演进后，默认线切到 Boot 4；但本项目后续要接 MyBatis-Plus、Security、LangChain4j 等，**3.x 生态更稳妥**，也更方便面试解释。

### 解决

不盲目跟默认 4.x，按等价参数手动搭建：

- Spring Boot **3.5.16**
- Java **21**
- Maven
- 首批依赖：Web / Validation / MySQL / Lombok（后续再换 MyBatis-Plus）

### 简历可写

> 面对 Spring Boot 4 默认脚手架，主动评估生态兼容性，选型并落地 Spring Boot 3.5.x + Java 21 工程骨架，避免后期组件冲突。

---

## 3. 引入数据源后必须配置 MySQL，否则启动失败

### 现象

空工程加了 MySQL / MyBatis 依赖后，直接启动会因找不到可用 DataSource 失败。

### 根因

Spring Boot 自动配置：classpath 上有 JDBC 相关依赖时，会尝试初始化数据源。

### 解决

1. 确认本机 MySQL 8 服务运行中
2. 创建业务库（最终使用 `ai_job_platform`）
3. 配置 `spring.datasource.*`
4. 先跑通连通性，再写业务表

### 简历可写

> 理解 Spring Boot 自动配置机制，完成 MySQL 数据源接入与启动链路打通。

---

## 4. 原生 MyBatis 与 MyBatis-Plus 不要同时引入

### 现象

初版使用 `mybatis-spring-boot-starter`；方案确定使用 MyBatis-Plus。

### 根因

MyBatis-Plus Starter 已包含 MyBatis 能力。若再叠原生 `mybatis-spring-boot-starter`，容易出现**版本冲突 / 自动配置冲突**。

### 解决

- 删除：`org.mybatis.spring.boot:mybatis-spring-boot-starter`
- 新增：`com.baomidou:mybatis-plus-spring-boot3-starter:3.5.17`（Boot 3 专用）
- 配置下划线转驼峰、SQL 日志
- `@MapperScan("com.aijob.server.mapper")`
- `UserMapper extends BaseMapper<User>`，先用内置 CRUD

### 简历可写

> 将原生 MyBatis 平滑切换为 MyBatis-Plus，规范依赖边界，基于 `BaseMapper` 快速落地用户表基础 CRUD。

---

## 5. `application.properties` 与 `application.yml` 迁移

### 现象

初期用 properties 做最小启动；后续统一改为 yml，并切换到正式库名。

### 处理

- 删除 `application.properties`
- 新建 `application.yml`
- 数据源指向 `ai_job_platform`
- 增加 MyBatis-Plus 配置：

```yaml
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
```

### 注意

库名曾短暂使用过 `ai_job`，最终统一为方案中的 **`ai_job_platform`**，避免「库建了但应用连错库」。

---

## 6. 启动成功但访问 `/` 出现 Whitelabel 404

### 现象

浏览器打开 `http://localhost:8080` 看到 Whitelabel Error Page / 404。

### 根因

不是启动失败，而是**还没有 Controller 映射根路径**。

### 验证方式

看日志是否有：

```text
Tomcat started on port 8080
Started AiJobServerApplication
```

有这两行，说明：

```text
Java 21 → Maven → Spring Boot → 内嵌 Tomcat
```

链路已通。

### 解决（验证接口）

新增 `HelloController`：

- `GET /api/hello` → `Hello AI Job Platform!`

### 简历可写

> 完成 Spring Boot 最小可运行验证，并实现首个 REST 接口，确认 HTTP 请求链路打通。

---

## 7. Windows PowerShell 执行带反引号的 MySQL DDL 失败

### 现象

在 PowerShell 里直接执行含 `` `user` `` 的 SQL，报语法错误。

### 根因

PowerShell 把反引号 `` ` `` 当作转义符，SQL 被破坏。

### 解决

将 DDL 写入 `sql/init_user.sql`，用 `cmd /c "mysql ... < init_user.sql"` 执行。

### 简历可写（偏工程细节，可选）

> 排查 Windows 终端转义导致的 SQL 执行失败，沉淀可重复执行的初始化脚本。

---

## 8. 重启服务时旧进程占用 / 退出码异常

### 现象

二次启动可能端口占用；杀掉旧进程后，旧任务显示 `exit_code=1`。

### 根因

本地开发反复 `spring-boot:run`，旧 Java 进程仍监听 8080；主动结束进程会导致非 0 退出码，**不等于业务代码错误**。

### 解决

重启前先释放 8080，再启动；以新进程日志中的 `Started ...` 为准。

---

## 9. 测试账号密码先用明文（后续必须改）

### 现状

`user` 表测试数据暂存明文 `123456`，仅用于验证库连接与查询。

### 后续方案（已在规划，勿写进简历成「已完成安全」）

```text
BCrypt + Spring Security + JWT
```

### 简历写法注意

可以说「预留鉴权与密码加密改造」；在真正做完前，不要写「已实现安全登录」。

---

## 当前已跑通的能力（截至本文）

```text
Java 21 + Maven 3.9
   ↓
Spring Boot 3.5.16
   ↓
MySQL 8（ai_job_platform.user）
   ↓
MyBatis-Plus
   ↓
HelloController（/api/hello）
```

包结构：

```text
com.aijob.server
├── controller
├── entity
├── mapper
├── service / impl
└── AiJobServerApplication
```

---

## 简历「项目难点」候选（可直接改写）

1. **多 JDK 环境冲突排查与 Java 21 统一**
2. **Spring Boot 3 技术选型（规避默认 Boot 4 带来的生态风险）**
3. **MyBatis → MyBatis-Plus 依赖治理与基础 CRUD 落地**
4. **Spring Boot 自动配置理解：数据源、内嵌 Tomcat、REST 首接口验证**

---

## 更新日志

| 日期 | 内容 |
|------|------|
| 2026-08-15 | 初稿：环境、脚手架、MySQL、MyBatis-Plus、首接口相关问题 |
