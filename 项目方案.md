# AI 智能求职与面试平台 — 项目方案（定稿）

> 状态：方案已定，**暂不编码**，等下一步命令再执行。  
> 更新：2026-08-14

---

## 1. 最终要做成什么

用户进入网站后的主流程：

```
                    AI智能求职与面试平台
                             │
          ┌──────────────────┼──────────────────┐
          ↓                  ↓                  ↓
       我的简历            岗位分析            AI面试
          │                  │                  │
       上传PDF             输入JD              选择岗位
          ↓                  ↓                  ↓
       AI解析              AI分析              AI提问
          ↓                  ↓                  ↓
       简历评分           岗位匹配度            AI追问
          │                  │                  │
          └──────────────────┼──────────────────┘
                             ↓
                        AI面试报告
                             │
                  ┌──────────┼──────────┐
                  ↓          ↓          ↓
                技能评分    薄弱点      学习建议
```

本项目可同时用于展示/投递方向：

- Java 开发
- Java 后端开发
- Vue 前端开发
- 全栈开发
- AI 应用开发

---

## 2. 第一版范围（不做微服务）

### 用户端

- 登录 / 注册
- 首页
- 个人中心
- 简历管理
- 岗位分析
- AI 模拟面试
- 面试历史
- 面试报告
- AI 知识库

### 管理端

- 用户管理
- 题库管理
- 知识库管理
- 面试记录
- AI 调用统计

**原则：第一版单体应用，不上微服务。**

---

## 3. 技术栈（已定）

### 后端

| 技术 | 说明 |
|------|------|
| Java 21 | 语言版本 |
| Spring Boot 3 | 应用框架 |
| MyBatis-Plus | ORM |
| MySQL 8 | 主库 |
| Redis | 缓存 / 会话 / Token |
| JWT | 认证 |
| Maven | 构建 |

### 前端

| 技术 | 说明 |
|------|------|
| Vue 3 | 框架 |
| TypeScript | 类型 |
| Vite | 构建 |
| Element Plus | UI |
| Pinia | 状态 |
| Axios | 请求 |
| ECharts | 图表（评分/报告） |

### AI

```
LangChain4j
    ↓
DeepSeek / 通义千问 / OpenAI 兼容接口
    ↓
RAG
    ↓
Embedding
    ↓
向量数据库
```

**选型理由：**

- 面向 JVM/Java 的 LLM 应用框架，官方提供 Spring Boot Starter
- 覆盖：AI Service、RAG、Tools/Function Calling、Agent、流式输出、聊天记忆
- 适合 Java + AI 应用岗位表述：

> 项目后端基于 Spring Boot 开发，使用 LangChain4j 对接大语言模型，实现 AI Service、RAG、Tool Calling 和流式输出。

---

## 4. 数据库设计（第一版，保持简单）

### 表清单

- `user`
- `resume`
- `job`
- `interview`
- `interview_message`
- `interview_report`
- `question`
- `knowledge_document`

### 字段草案

#### user

| 字段 | 说明 |
|------|------|
| id | 主键 |
| username | 用户名 |
| password | 密码 |
| email | 邮箱 |
| role | 角色 |
| create_time | 创建时间 |
| update_time | 更新时间 |

#### resume

| 字段 | 说明 |
|------|------|
| id | 主键 |
| user_id | 用户 |
| file_name | 文件名 |
| file_url | 文件地址 |
| resume_text | 解析文本 |
| ai_analysis | AI 分析结果 |
| score | 评分 |
| create_time | 创建时间 |

#### job

| 字段 | 说明 |
|------|------|
| id | 主键 |
| user_id | 用户 |
| company | 公司 |
| position | 岗位 |
| description | JD 描述 |
| match_score | 匹配度 |
| analysis | AI 分析 |
| create_time | 创建时间 |

#### interview

| 字段 | 说明 |
|------|------|
| id | 主键 |
| user_id | 用户 |
| position | 面试岗位 |
| status | 状态 |
| score | 总分 |
| start_time | 开始时间 |
| end_time | 结束时间 |

#### interview_message

| 字段 | 说明 |
|------|------|
| id | 主键 |
| interview_id | 面试 ID |
| role | 角色（AI / 用户） |
| content | 内容 |
| create_time | 创建时间 |

#### interview_report

| 字段 | 说明 |
|------|------|
| id | 主键 |
| interview_id | 面试 ID |
| total_score | 总分 |
| java_score | Java 分 |
| mysql_score | MySQL 分 |
| redis_score | Redis 分 |
| spring_score | Spring 分 |
| weak_points | 薄弱点 |
| suggestions | 学习建议 |
| create_time | 创建时间 |

> `question`、`knowledge_document` 第二阶段/管理端再细化字段。

---

## 5. Redis 用途（必须真实落地）

第一阶段用途：

```
Redis
 ├── 用户登录 Token
 ├── 热门面试题缓存
 ├── AI 面试会话
 └── 防止重复提交
```

面试会话流程：

```
用户进入面试
      ↓
Redis 保存面试 Session
      ↓
AI 开始提问
      ↓
用户回答
      ↓
Redis 暂存上下文
      ↓
最终保存 MySQL
```

**面试可讲点：** AI 面试是高频会话场景，会话态用 Redis 暂存，降低 MySQL 访问压力；结束后再落库。

---

## 6. AI 三大核心功能

### 6.1 AI 简历分析

```
PDF → 文本解析 → LLM → 结构化 JSON → MySQL
```

输出示例：

- 技能星级（Java / Python / Vue 等）
- 项目经历、技术深度评分
- 主要问题列表
- 改进建议（补充 Spring Boot、Redis、Docker 等）

### 6.2 JD 岗位匹配

用户粘贴 Boss 直聘等 JD → 与简历匹配 → 输出：

- 岗位匹配度（如 82%）
- 已掌握技能
- 需补充技能
- 备考建议

后续可升级为 Embedding + RAG。

### 6.3 AI 模拟面试（核心）

```
AI 面试 Agent
      ↓
分析回答
      ↓
判断掌握程度
      ↓
选择下一道问题
      ↓
继续追问
```

不是简单单轮聊天，而是多轮追问 + 评分 + 报告。

---

## 7. RAG 知识库（第二阶段）

上传资料（Java / Redis / MySQL / Spring / 计算机网络等 PDF）：

```
PDF → 文本解析 → Chunk → Embedding → 向量库
```

问答流程：

```
Query → Embedding → 向量检索 Top-K → LLM → 回答
```

LangChain4j 官方 RAG：文档索引 + 检索；可从基础 RAG 升级到查询转换、路由、重排序。

---

## 8. SSE 流式输出（必须做）

避免整段等待后一次性返回，改为逐步输出。

```
Vue → SSE → Spring Boot → LangChain4j → LLM
```

LangChain4j 支持流式响应，第一版即纳入。

---

## 9. 项目目录结构

```
ai-job-platform/
│
├── ai-job-server/
│   ├── controller/
│   ├── service/
│   ├── service/impl/
│   ├── mapper/
│   ├── entity/
│   ├── dto/
│   ├── vo/
│   ├── config/
│   ├── security/
│   ├── ai/
│   ├── rag/
│   ├── exception/
│   └── utils/
│
└── ai-job-web/
    ├── src/
    │   ├── api/
    │   ├── components/
    │   ├── views/
    │   ├── router/
    │   ├── stores/
    │   └── utils/
```

> 当前仓库根目录：`AI-Intelligent-Job-Hunting`，落地时按上述结构建子目录。

---

## 10. 五阶段开发计划

### 第一阶段：Java 基础业务

```
Spring Boot → MySQL → MyBatis-Plus → Vue3 → 登录注册 → 简历管理
```

先把普通网站跑起来。

### 第二阶段：接入 AI

```
Spring Boot → LangChain4j → DeepSeek
```

完成：AI 简历分析、JD 分析、AI 聊天。

### 第三阶段：AI 面试

```
AI 面试 → 多轮对话 → 上下文记忆 → AI 评分 → 面试报告
```

### 第四阶段：RAG

```
PDF → 解析 → Chunk → Embedding → Vector DB → RAG
```

### 第五阶段：工程化

```
Redis / Elasticsearch / Docker / Nginx / 日志 / 异常处理 / 接口测试 / GitHub Actions
```

完成后的能力定位：

> 使用 Java/Spring Boot 构建了具有 RAG、AI Agent、流式对话、缓存和工程化部署能力的 AI 应用。

---

## 11. 简历项目描述（目标效果）

**项目名称：** AI 智能求职与面试平台

**技术栈：** Java 21、Spring Boot 3、Vue3、MySQL、Redis、MyBatis-Plus、LangChain4j、RAG、SSE、Elasticsearch、Docker

**项目描述：** 面向求职者开发的智能求职与面试平台，实现简历解析、岗位匹配、AI 模拟面试、知识库问答及面试报告生成等功能。

---

## 12. 执行约定

- [x] 项目方案文档已落盘
- [ ] **未开始写代码** — 等明确命令后再执行第一阶段
